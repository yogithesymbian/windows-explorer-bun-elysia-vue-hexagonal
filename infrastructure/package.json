{
  "name": "explorer-infrastructure",
  "version": "1.0.1",
  "scripts": {
    "migrate": "bun run db/migrate.ts",
    "seed": "bun run db/seed.ts",
    "db:client": "bun run db/client.ts",
    "db:gen": "bun x drizzle-kit generate",
    "db:gen:custom": "bun x drizzle-kit generate --custom --name=ltree-fts"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.6"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.31.5"
  }
}